# Attestor: Ethereum private key used to sign claims.
# Generate one with: node -e "import('viem/accounts').then(m => console.log(m.generatePrivateKey()))"
PRIVATE_KEY=0x_your_attestor_private_key_here

# Gateway
ATTESTOR_URL=ws://attestor:8001/ws
PORT=5177
ZK_ENGINE=gnark
